import {
  "moonbitlang/x/sys",
  "moonbitlang/x/fs",
}

supported_targets = "+js+native"

options(
  "native-stub": [ "which_native.c" ],
  targets: {
    "which_js.mbt": [ "js" ],
    "which_native.mbt": [ "native", "llvm" ],
    "which_wasm.mbt": [ "wasm", "wasm-gc" ],
  },
)
